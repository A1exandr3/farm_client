package isometric
{
    import flash.geom.Point;

    public class IsoPoint extends Point
    {
        public function IsoPoint(x:Number = 0, y:Number = 0)
        {
            super(x, y)
        }
    }
}

